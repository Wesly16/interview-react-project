import { CardLogin } from "../components";
import { MainLayout } from "../Layout";

const Login = () => {
  return (
    <MainLayout>
      <CardLogin />
    </MainLayout>
  );
};

export default Login;
